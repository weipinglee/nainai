<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
<title></title>

<link href="layers/css/style.css" rel="stylesheet" type="text/css" >

</head>
<body>

  <div id="info_return">
    <div class="content">
      <p>恭喜您，购买成功！</p>
      <!-- <p>支付失败，请<a href="" class="button gray">重新支付</a></p> -->
      <p class="xinxi">您已经成功购买<a href="">{$order.product_name}</a>&nbsp;&nbsp;&nbsp;订单号：<span class="red">{$order.order_no}</span></p>
    </div>
    <div class="detail">
      <div class="detail_box">
        <h4>订单详情</h4>
        <p>
          <span class="dl">订单号：</span><span class="dr">{$order.order_no}</span>
        </p>
        <p>
          <span class="dl">商品名称：</span><span class="dr">{$order.product_name}</span>
        </p>
        <p>
          <span class="dl">支付金额：</span><span class="dr">￥{$order.order_total_fee}</span>
        </p>
        {if $order.invo_type==0}
        <p>
          <span class="dl">发票明细：</span><span class="dr">发票抬头：{$order.invo_title}&nbsp;&nbsp;&nbsp;&nbsp;开票金额：￥{$order.order_total_fee}</span>
         </p>
        {/if}
      </div>
      {if $order.invo_type==1}
            <div class="detail_box">
              <h4>发票详情</h4>
              <p>
                <span class="dl">单位名称：</span><span class="dr">{$order.invo_company}</span>
              </p>
              <p>
                <span class="dl">纳税人识别号：</span><span class="dr">{$order.invo_taxno}</span>
              </p>
              <p>
                <span class="dl">注册地址：</span><span class="dr">￥{$order.invo_address}</span>
              </p>
              <p>
                  <span class="dl">注册电话：</span><span class="dr">￥{$order.invo_phone}</span>
                </p>
                <p>
                  <span class="dl">开户银行：</span><span class="dr">￥{$order.invo_bank}</span>
                </p>

              <p>
                <span class="dl">银行账户：</span><span class="dr">￥{$order.invo_account}</span>
              </p>

            </div>
        {/if}
      <p class="warn"><span class="red">温馨提示：</span>您需要的发票会在5天内由快递送达。</p>
    </div>
    <div class="info">
      <p>付款单位：{$order.buyer_company}</p>
      <p>联系电话：{$order.buyer_phone}</p>
    </div>
    <div class="footer">
      <a href="" class="button red">打印当前页面</a>
      <a href="" class="button">关闭页面</a>
    </div>
  </div>


</body>
</html>